import React from 'react';
import AdminStudentAssigning from '../Admin/StudentAssigning';

const StudentAssigning = (props) => {
  return <AdminStudentAssigning {...props} />;
};

export default StudentAssigning;
