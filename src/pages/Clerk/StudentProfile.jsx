import React from 'react';
import AdminStudentProfile from '../Admin/StudentProfile';

const StudentProfile = (props) => {
  return <AdminStudentProfile {...props} />;
};

export default StudentProfile;
