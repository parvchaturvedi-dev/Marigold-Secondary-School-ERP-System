import React from 'react';
import AdminApplicationInbox from '../../components/common/AdminApplicationInbox';

const Application = ({ session }) => {
  return <AdminApplicationInbox session={session} />;
};

export default Application;
