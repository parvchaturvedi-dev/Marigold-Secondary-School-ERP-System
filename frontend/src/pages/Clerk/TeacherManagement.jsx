import React from 'react';
import AdminTeacherManagement from '../Admin/TeacherManagement';

const TeacherManagement = (props) => {
  return <AdminTeacherManagement {...props} />;
};

export default TeacherManagement;
