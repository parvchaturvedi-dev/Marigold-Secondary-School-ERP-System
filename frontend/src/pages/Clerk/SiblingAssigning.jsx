import React from 'react';
import AdminSiblingAssigning from '../Admin/SiblingAssigning';

const SiblingAssigning = (props) => {
  return <AdminSiblingAssigning {...props} />;
};

export default SiblingAssigning;
