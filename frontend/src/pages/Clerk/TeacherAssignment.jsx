import React from 'react';
import AdminTeacherAssignment from '../Admin/TeacherAssignment';

const TeacherAssignment = (props) => {
  return <AdminTeacherAssignment {...props} />;
};

export default TeacherAssignment;
