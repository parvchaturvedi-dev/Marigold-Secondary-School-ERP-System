import React from 'react';
import JitsiMeetingHub from '../../components/common/JitsiMeetingHub';

const Meetings = ({ session }) => <JitsiMeetingHub session={session} role="clerk" />;

export default Meetings;
