import React from 'react';
import AdminStudentManagement from '../Admin/StudentManagement';

const StudentManagement = (props) => {
  return <AdminStudentManagement {...props} />;
};

export default StudentManagement;
