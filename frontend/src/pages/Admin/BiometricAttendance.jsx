import React from 'react';
import AttendanceControl from '../../components/common/AttendanceControl';

const BiometricAttendance = (props) => <AttendanceControl {...props} role="admin" />;

export default BiometricAttendance;
