import React from 'react';
import JitsiMeetingHub from '../../components/common/JitsiMeetingHub';

const Meetings = ({ session }) => <JitsiMeetingHub session={session} role="teacher" />;

export default Meetings;
